/*
 * Copyright 2023-2024 Amazon.com, Inc. or its affiliates.
 */

import * as indexes from "../lib/index";

describe("osml-cdk-constructs index.ts", () => {
  test("osml-cdk-constructs index.ts to be defined", () => {
    expect(indexes).toBeDefined();
  });
});
